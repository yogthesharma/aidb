[package]
name = "aidb-ffi"
description = "C ABI for TypeScript and Python bindings. Same engine, no second store."
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "aidb_ffi"
crate-type = ["cdylib", "rlib"]

[dependencies]
aidb.workspace = true
serde_json.workspace = true
