[package]
name = "aidb-cli"
description = "CLI binary `aidb`: sql, runs, and optional serve over the same file."
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "aidb"
path = "src/main.rs"

[dependencies]
aidb.workspace = true
aidb-server.workspace = true
